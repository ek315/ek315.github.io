<div class="col">
  <a href="{% if personalproject.redirect %}{{ personalproject.redirect }}{% else %}{{ personalproject.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable">
      {% if personalproject.img %}
        {%
          include figure.liquid
          loading="eager"
          path=personalproject.img
          sizes = "250px"
          alt="project thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body">
        <h2 class="card-title" style="font-size: 1.2em;">{{ personalproject.title }}</h2>
        <p class="card-text">{{ personalproject.description }}</p>
        <div class="row ml-1 mr-1 p-0">
          {% if personalproject.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ project.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if personalproject.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ personalproject.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
